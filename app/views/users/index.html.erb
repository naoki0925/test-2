<div>
	<h2>User</h2>
	<thead>
		<tr>
			<th>image</th>
			<th>name</th>
			<th colspan="3"></th>
		</tr>
	</thead>
	<tbody>
		<% @users.each do |user| %>
			<tr><%= link_to(user.name, "/users/#{user.id}") %></tr>
		<% end %>
	</tbody>
</div>